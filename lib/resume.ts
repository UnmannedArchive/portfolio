/**
 * Single resume PDF in /public — use your capstone CV as this file.
 * Suggested path: public/joseph-solomon-resume.pdf
 */
export const resume = {
  href: "/joseph-solomon-resume.pdf",
  label: "Resume",
  fileName: "Joseph-Solomon-Resume.pdf",
  /** Re-check after replacing the file (`wc -c public/joseph-solomon-resume.pdf`). */
  sizeLabel: "~150 KB",
} as const;
